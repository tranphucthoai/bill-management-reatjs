import React from 'react';
import PropTypes from 'prop-types';
import './style.scss'

SideBar.propTypes = {

};

function SideBar(props) {
    return (
        <aside>

        </aside>
    );
}

export default SideBar;